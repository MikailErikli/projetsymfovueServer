{% extends 'layouts/base.html.twig' %}

{%  block title 'Profile' %}


{% block body %}

    <div class="row">
        <div class="col-md-6 mx-auto text-center">
            <h1 class="my-5">Profile</h1>

            <img src="{{ asset('images/log.png') }}" alt="Symfony!" style="height: 300px;width: 300px;" class="shadow rounded-circle"/>

            <p class=" mt-3 lead">{{ app.user.email }}</p>
            <br/>
            <h2>{{ app.user.fullName }}</h2>
            <br/>
            <p> <a href="{{ path ('app_account_edit') }}">Edit Account</a> | <a href="">Change Password</a></p>


        </div>
    </div>
{% endblock %}
